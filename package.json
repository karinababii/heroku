{
  "name": "heroku",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/cypress run --spec 'cypress/integration/test.spec.js' --reporter mocha-allure-reporter",
    "report": "./node_modules/.bin/allure generate allure-results --clean -o allure-report",
    "open": "./node_modules/.bin/cypress open",
    "wipe": "rm -r allure-results/ allure-report/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress-file-upload": "^5.0.8",
    "mocha": "^9.2.0"
  },
  "dependencies": {
    "mocha-allure-reporter": "^1.4.0"
  }
}
